{
  "name": "dns-doctor",
  "version": "0.1.0",
  "description": "A tiny CLI that diagnoses DNS for common hosting providers and prints exact fix steps.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "dns-doctor": "./dist/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "test": "node --test",
    "lint": "node -e \"console.log('No lint configured')\""
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
